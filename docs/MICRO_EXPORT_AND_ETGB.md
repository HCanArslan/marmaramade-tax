# Micro-export and ETGB

Each micro-export case links the local order, exporter, shipment, invoice/proforma, customs evidence, and ETGB status. ShipEntegra API v4.0.4 does not document ETGB file retrieval, so ETGB uses a manual private upload and remains pending until verified.
