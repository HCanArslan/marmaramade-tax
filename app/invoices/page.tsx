import { createSalesDocumentAction } from "@/app/actions/operations";import { requireAdmin } from "@/lib/auth/require-admin";import { prisma } from "@/lib/prisma";
export default async function InvoicesPage(){await requireAdmin({redirectTo:"/invoices"});const [orders,documents]=await Promise.all([prisma.order.findMany({orderBy:{orderDate:"desc"},take:100}),prisma.salesDocument.findMany({orderBy:{createdAt:"desc"},take:100})]);return <div className="mx-auto max-w-6xl space-y-6"><header><p className="eyebrow">Draft records, not unsupported issuance</p><h1 className="mt-2 text-3xl font-semibold">Invoices & sales documents</h1></header><div className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Document type, VAT treatment, issuer identity, and provider submission must be confirmed. This module does not call an undocumented invoice API.</div><section className="card p-5"><h2 className="font-semibold">Create draft</h2><form action={createSalesDocumentAction} className="mt-4 grid gap-3 sm:grid-cols-4"><select className="field" name="orderId"><option value="">No order link</option>{orders.map(o=><option value={o.id} key={o.id}>{o.orderNumber}</option>)}</select><I n="documentType" p="Type" v="E_ARCHIVE_DRAFT"/><I n="documentNumber" p="Document number" r={false}/><I n="currency" p="Currency" v="USD"/><I n="netAmount" p="Net"/><I n="vatAmount" p="VAT" v="0"/><I n="grossAmount" p="Gross"/><button className="rounded-xl bg-jade px-4 py-2 text-sm text-white">Create draft</button></form></section><section className="card overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead><tr className="border-b bg-stone-50"><th className="p-4">Document</th><th>Status</th><th>Order</th><th>Total</th><th>Validation</th></tr></thead><tbody>{documents.map(d=><tr className="border-b" key={d.id}><td className="p-4">{d.documentType} · {d.documentNumber||"unnumbered"}</td><td>{d.status}</td><td>{d.orderId||"—"}</td><td>{d.grossAmount.toFixed(2)} {d.currency}</td><td>Professional confirmation required</td></tr>)}</tbody></table></section></div>};const I=({n,p,v,r=true}:{n:string;p:string;v?:string;r?:boolean})=><input className="field" name={n} placeholder={p} defaultValue={v} required={r}/>;
